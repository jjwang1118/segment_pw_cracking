# 修改紀錄 — 2026-07

本檔由 `docs/logs/202607NN_modify.md` 共 14 份逐日紀錄（其中 `20260715_modify.md` 內含 07-15、07-17 兩日）合併而成，依日期由舊到新排列，內容未經改寫。

- [2026-07-04](#2026-07-04)
- [2026-07-10](#2026-07-10)
- [2026-07-12](#2026-07-12)
- [2026-07-13](#2026-07-13)
- [2026-07-14](#2026-07-14)
- [2026-07-15](#2026-07-15)
- [2026-07-17](#2026-07-17)
- [2026-07-18](#2026-07-18)
- [2026-07-20](#2026-07-20)
- [2026-07-21](#2026-07-21)
- [2026-07-22](#2026-07-22)
- [2026-07-25](#2026-07-25)
- [2026-07-27](#2026-07-27)
- [2026-07-28](#2026-07-28)
- [2026-07-30](#2026-07-30)

---

## 2026-07-04

### PassLLM Bug 修正

#### Fix 1 — `src/utils/tokenize.py`

`process_val_targeted` 的參數名稱 `limit_vocab` 與 caller（`train.py` 傳 `vocab=`）及 callee（`process_train_targeted` 期望 `vocab=`）不一致，改為 `vocab`。

#### Fix 2 — `src/model/train.py`

`Dataset.from_pandas(data, split=["train"])` 的 `split` 型態錯誤，改為字串 `split="train"`。

詳細說明見 `docs/passllm/bug_fixes.md`。

#### Fix 3 — `src/search/search.py`

`predict_next` 錯誤回傳 3 個值（加入 contrastive 時殘留），所有 trawling callers（`_width_search`、`get_prefix`）只解包 2 個 → crash。移除 `output_hidden_states=True` 與第三個回傳值。

#### Fix 4 — `config/evaluation_126_csdn_config.ini`、`config/evaluation_126_csdn_contrastive_config.ini`

`prompmt_template_id`（typo）改為 `prompt_template_id`，讓 config 設定值實際生效。

#### Fix 5 — `src/utils/utils.py`、`src/model/eval.py`

評估結束無最終彙整：在 `pm_guesser` 加 `finalize()` 方法（沖出剩餘 entries + 印出 crack rate 彙整），並在 `GuessLLM_Evaluator.eval()` 結尾呼叫。

---

## 2026-07-10

### 新增：COMB（外部已切分）資料集的貼標籤流程

#### 背景

新增 `datasets/processed/COMB/split/{train,test}.txt`，是已經由外部流程切好 train/test 的原始密碼清單（未清洗、未貼標籤），與現有 `run_pcfg_segment.py`「先全量貼標籤 → 再抽樣切 train/test」的順序相反，無法直接套用既有腳本。

#### 新增檔案 — `run_pcfg_tag_presplit.py`

在不重新抽樣、不打亂既有 train/test 歸屬的前提下，對已切分好的資料集進行清洗 + 貼標籤：

1. **移除含 `|` 的密碼**：密碼原文若含 `|`，會與 `Tokens`/`Tags` 欄位用來 join 的分隔符衝突，造成無法還原的壞資料（例：`"sssham80 | 1"` → `Tokens: "ss|sham|80| | |1"`），故直接剔除
2. **移除 PW_WORD 字元集以外的密碼**：比對 `util/pw_tokenize.py:get_alpa()` 的 `PW_WORD`（模型實際可編碼/生成的 94 字元表）
3. **內部去重**：`train.txt`、`test.txt` 各自 `drop_duplicates`（沿用 `util/data.py` 的 `remove_duplicates` 邏輯）
4. **移除 train/test 洩漏**：從 `test.txt` 中移除同時存在於 `train.txt` 的密碼
5. **貼標籤**：沿用 `src/PCFGSegment.py` 的 `PCFGSegmenter`，對 `pos` / `backoff` / `pos_semantic` 三種 tagtype 分別處理 train / test
6. **輸出**：
   - 中繼 CSV：`gen/semanticPCFG/COMB_{split}_{tagtype}_tagged.csv`
   - 最終 JSONL：`datasets/processed/semanticPCFG/COMB/{tagtype}/split/{train,test}_data.jsonl`（欄位對齊既有格式：`Password, Tokens, Tags, source`）

未修改 `config/pcfg_segment.yaml`（依使用者要求，presplit 相關路徑直接寫死在腳本內，不污染共用 config）。

#### 修正：字元集檢查基準從 `util/data.py` 改為 `PW_WORD`

一開始誤用 `config/config.yaml` 的 `password_cleaning.allowed_charsets`（`util/data.py:clean_data()` 的規則）做字元集過濾，移除了 311 筆含空格的密碼（train 303、test 8）。後來發現這規則跟訓練/推論階段實際使用的字元表 `util/pw_tokenize.py:get_alpa()` 的 `PW_WORD` **不一致**：

- `PW_WORD`（94 字元，模型真正能編碼/生成的字元）：**允許空格**、**不允許冒號 `:`**
- `util/data.py` 的 `allowed_charsets`：**不允許空格**、**允許冒號 `:`**

`PW_WORD` 才是正確的判斷基準，因為它直接對應 `get_alpa()` 建立的 token-ID 對照表。已改用 `PW_WORD` 重新過濾並確認 COMB 原始資料中沒有冒號，因此唯一的實際影響是把先前誤刪的 311 筆含空格密碼恢復。

#### 執行結果（dataset=COMB，最終版）

| 階段 | train | test |
|---|---|---|
| 原始筆數 | 284,072 | 14,950 |
| 移除含 `\|` 密碼後 | 284,016 | 14,949 |
| 移除 PW_WORD 以外字元後 | 284,016 | 14,949 |
| 內部去重後 | 262,265 | 14,555 |
| 移除 train/test 重疊後 | 262,265 | 13,513 |
| 長度過濾 [8,20] 後（三種 tagtype 皆同） | 262,263 | 13,513 |

- 含 `|` 的密碼：train 56 筆、test 1 筆（0.02%），已剔除
- PW_WORD 以外字元：0 筆（COMB 原始資料未含冒號等 PW_WORD 排除的字元）
- 含空格的合法密碼：train 303 筆、test 8 筆，保留
- train/test 重疊移除：14,555 → 13,513
- 事後驗證：三種 tagtype 的 train/test JSONL 均無 `|`、均無 PW_WORD 以外字元、train/test 密碼集合交集為 0
- 三種 tagtype（pos / backoff / pos_semantic）皆已產生，執行時間約 1m46s（含 `--force` 重跑）

#### 用法

```bash
python run_pcfg_tag_presplit.py                    # dataset=COMB，全部 tagtype
python run_pcfg_tag_presplit.py --dataset COMB --tagtype backoff
python run_pcfg_tag_presplit.py --force             # 強制重跑（忽略既有 CSV cache）
```

---

## 2026-07-12

### 新增：Prompt Template id=6（Template F，plain-text inline tag structure）

#### 背景

沿用 id=5 的 tag 結構表示法（`<tag1><tag2>...`），但拿掉 JSON 包裝，改成 system prompt 後接一個字面換行再直接接 inline tag 字串，格式更貼近純文字對話：

```
As a targeted password guessing model, your task is to utilize the provided
structure information to guess the corresponding password.
<mname><char1><number5>
```

#### 修改檔案

- `src/prompt_template.py`
  - `_get_indice()` 新增 `id == 6` 分支，回傳新的 system prompt 文字
  - 新增 `prompt_convert_inline_plain()`（Template F）：組出 `template + "\n" + "<tag1><tag2>..."`，不經過 `json.dumps`
- `run_eval.py`：`_build_prompt()` 新增 `template_id == 6` 分支，呼叫 `prompt_convert_inline_plain`
- `util/pw_tokenize.py`：`process_train_targeted()` 新增 `template_id == 6` 分支，`knowledge_text = "\n" + structure`（訓練 prompt 與推論 prompt 一致，比照 id=5 的做法）

#### 未修改

- `run_search.py` 的 `_get_indice(template_id)` 呼叫方式維持原樣（該檔案本來就不會把 tag 結構組進 prompt，屬既有行為，不在本次異動範圍）
- `config/search.yaml` 尚未新增對應區塊，待使用者決定是否要跑 id=6 的訓練/評估再補上

---

## 2026-07-13

### 新增比較報告：Prompt Template id=5（run_7）vs id=6（run_12）

- **新增** `docs/reports/comparison_id5run7_vs_id6run12_Mistral-7B_constrained_beam_search.md`
  - 比較 Mistral-7B-v0.1 兩個 prompt template：id=5（run_7，來源 log `results/eval/eval-249332.out`）vs id=6（run_12，來源 log `results/eval/eval-260704.out`）
  - 兩次評估皆使用相同測試集（000webhost backoff split，5,000 筆）
  - 內容涵蓋：prompt 設計差異（id=5 為 JSON 包裝 + 詳細指示；id=6 為純文字 + 精簡指示）、crack rate @K 對照、tag 類型破解率對照
  - @1000: id=5（run_7）=14.76% vs id=6（run_12）=14.88%（Δ +0.12pp，屬抽樣誤差範圍），四個 K 值差距皆在隨機波動範圍內
  - tag 類型破解率計算方式：解析 log 中 `[fallback→dynamic]` 標記判斷 backoff vs pos/pos_semantic（65/673 for run_7，67/677 for run_12）
  - **修正**：直接讀取 `checkpoints/Mistral-7B-v0.1/run_7/lora_final/adapter_config.json` 確認 run_7 實際為 r=16/alpha=32/target_modules=[q,k,v]（`train_config.yaml` 預設值），**並非**先前 `comparison_run6_vs_run7_Mistral-7B_id5_constrained_beam_search.md` 記錄的 r=32/alpha=64（該加大設定僅 run_6 使用，未修改舊報告本身，僅於本次新報告中註記更正）。因此本次比較的 LoRA 容量在 run_7 與 run_12 之間完全一致，排除了先前版本（誤用 run_6 資料）的 LoRA 混淆變因

- **新增** `gen/results/comparison_id5run7_vs_id6run12_Mistral-7B_constrained_beam_search_result.png`
  - 左圖：兩條 crack rate 曲線疊加（run_7 id=5 藍 vs run_12 id=6 橘）
  - 右圖：各 tag 類型破解率 grouped bar chart
  - 沿用既有比較報告配色與版面風格（`comparison_run6_vs_run7_Mistral-7B_id5_constrained_beam_search_result.png`）

- **移除**（誤用 eval-249331/run_6 資料的初版，已由上述 run_7 版本取代）：`docs/reports/comparison_id5run6_vs_id6run12_Mistral-7B_constrained_beam_search.md`、`gen/results/comparison_id5run6_vs_id6run12_Mistral-7B_constrained_beam_search_result.png`

### 新增單次評估報告（簡明版）：id=6 run_12、id=5 run_10（COMB）

- **新增** `docs/reports/id6_run12_Mistral-7B_id6_constrained_beam_search.md` + `gen/results/id6_run12_Mistral-7B_id6_constrained_beam_search_result.png`
  - 來源：`results/eval/eval-260704.out`；000webhost backoff 測試集，5,000 筆
  - Crack rate @1000: 744/5000 (14.88%)；破解密碼 tag 分佈：純 backoff 67 筆 (9.0%)，含 pos/pos_semantic 677 筆 (91.0%)

- **新增** `docs/reports/id5_run10_Mistral-7B_id5_COMB_constrained_beam_search.md` + `gen/results/id5_run10_Mistral-7B_id5_COMB_constrained_beam_search_result.png`
  - 來源：`results/eval/eval-258985.out`；COMB backoff 測試集（訓練/測試皆 COMB，非 000webhost），5,000 筆
  - Crack rate @1000: 851/5000 (17.02%)；破解密碼 tag 分佈：純 backoff 44 筆 (5.2%)，含 pos/pos_semantic 807 筆 (94.8%)
  - 與既有的 `id5_run10_Mistral-7B_id5_constrained_beam_search.md`（訓練/評估參數詳盡記錄版）互補，本篇為 crack rate + 圖表 + tag 分佈的精簡版

---

## 2026-07-14

### 新增分析：PassLLM 猜中密碼的姊妹密碼組成（COMB）

- **修改** `docs/reports/comparison_PassLLM_vs_PCFG-LLM_COMB.md`
  - 新增第 6 節「PassLLM 猜中密碼的姊妹密碼組成分析」
  - 資料來源：`gen/passllm/run_01/input_output.jsonl`（依 `model_input` 中 `"Old password"` 欄位是否為空陣列判定有無姊妹密碼，依密碼去重後 5,000 筆，與既有報告一致）
  - 結果：@1000 猜中的 1,054 筆中，1,051 筆（99.7%）帳號有姊妹密碼，僅 3 筆無姊妹密碼；@10/@50 猜中密碼 100% 皆來自有姊妹密碼帳號
  - 結論：PassLLM 高度依賴同帳號舊密碼線索，與本研究 PCFG-LLM（不依賴舊密碼、僅憑結構 tag）鎖定不同威脅情境

- **新增** `gen/results/comparison_PassLLM_vs_PCFG-LLM_COMB_sister_breakdown_result.png`
  - 長條圖：橫軸為 @10/@50/@100/@500/@1000，堆疊長條分解「有姊妹密碼被猜中」與「無姊妹密碼被猜中」數量，無姊妹密碼區段以引線標註實際數值
  - 沿用既有結果圖表配色與版面風格（seaborn deep palette 藍／橘、淺灰背景、虛線格線）

### 新增三方比較：PassLLM 新／舊 Prompt vs 本研究 PCFG-LLM（COMB）

- **修改** `docs/reports/comparison_PassLLM_vs_PCFG-LLM_COMB.md`
  - 新增第 7 節「三方比較：PassLLM 新／舊 Prompt vs 本研究 PCFG-LLM」
  - PassLLM 新增 `gen/passllm/run_02/`（新 prompt：移除 `Old password` 的 JSON 包裝，姊妹密碼改以 `</s>` 逐一串接），與既有 run_01（舊 prompt：`json.dumps` 包裝）對照；兩次評估共用同一 checkpoint（`checkpoints/mistral_7b_COMB/final`）與同一評估設定，僅 prompt 內容格式不同
  - **資料處理注意**：`gen/passllm/run_02/passllm_run2_COMB.json` 實為 run_01 全部 5,054 筆 + run_02 自己 5,000 筆串接而成（共 10,054 筆），已取檔案末尾 5,000 筆（真正 run_02 資料）計算，並與 `gen/passllm/run_02/eval-260642_params_summary.md` 官方數字（650/5000, 13.00% @1000）核對一致
  - 結果：@1000 crack rate 三方為 PassLLM 舊 prompt 21.08% > 本研究 PCFG-LLM 17.02% > PassLLM 新 prompt 13.00%；同一 checkpoint 僅換推論 prompt 格式即造成 −8.08pp 的大幅衰退，顯示 PassLLM targeted 猜測對 prompt 字面格式高度敏感

- **新增** `gen/results/comparison_PassLLM_oldnew_vs_PCFG-LLM_COMB_result.png`
  - 折線圖：三條 crack rate 曲線（本研究 PCFG-LLM 藍／PassLLM 舊 prompt 橘／PassLLM 新 prompt 綠），沿用既有 `comparison_PassLLM_vs_PCFG-LLM_COMB_result.png` 的 tab10 配色與版面風格

### 附註：新 Prompt 格式出處標註

- **修改** `docs/reports/comparison_PassLLM_vs_PCFG-LLM_COMB.md` 第 7 節
  - 於「新 Prompt（run_02）」表格欄位加註腳注 `[^passllm-format]`，說明姊妹密碼 `</s>` 串接格式參考 PassLLM 原始設計（非本專案自訂）；圖表下方另加一行引用說明
  - 依使用者指示，此處僅標註來源為「PassLLM」，未附完整論文引用資訊（尚無提供論文標題/作者/連結）
- **修改** `gen/results/comparison_PassLLM_oldnew_vs_PCFG-LLM_COMB_result.png`
  - 圖片右下角加註小字：「＊「新 Prompt」姊妹密碼 `</s>` 串接格式參考 PassLLM 原始設計」

### 擴充為四方比較：加入本研究自己的新／舊 Prompt（id=6 run_13 vs id=5 run_10）

- **修改** `docs/reports/comparison_PassLLM_vs_PCFG-LLM_COMB.md`
  - 第 7 節標題改為「四方比較：PassLLM 新／舊 Prompt vs 本研究 PCFG-LLM 新／舊 Prompt」
  - 新增本研究端 `run_13`（prompt_template_id=6，來源 [results/eval/eval-261839.out](../../results/eval/eval-261839.out)，對應 `gen/eval_results_id6_run_13_Mistral7B_id6_COMB.jsonl`），對照既有 `run_10`（id=5）；兩者皆為 COMB 資料集、各自獨立訓練的 LoRA（r=16/alpha=32/q,k,v_proj 相同，僅 prompt_template_id 不同）
  - crack rate 四方對照表擴充為 4 欄（本研究舊/新 Prompt、PassLLM 舊/新 Prompt），@50/@500 數字由 `gen/eval_results_id6_run_13_Mistral7B_id6_COMB.jsonl` 逐筆 `min_cracked_guess_number` 計算，與 log 內 @1/@10/@100/@1000 官方數字核對一致（159/348/576/835 @ 1000筆=5000）
  - 結論：PassLLM 新／舊 prompt crack@1000 差距達 −8.08pp（高度敏感），本研究新／舊 prompt 差距僅 −0.32pp（17.02%→16.70%，可視為訓練隨機波動），顯示本研究 tag 結構化 prompt 對格式變動的穩健性遠高於 PassLLM 的舊密碼線索格式
  - **注意（先前確認過的差異）**：一開始誤用 `results/eval/eval-260704.out`（實為 000webhost 測試集、run_12），經使用者更正後改用 `results/eval/eval-261839.out`（COMB 測試集、run_13，檔名含 `_COMB` 可驗證）

- **新增** `gen/results/comparison_PassLLM_oldnew_vs_PCFG-LLM_oldnew_COMB_result.png`（四方折線圖，取代舊三方圖）
  - 配色沿用系統既有慣例：本研究＝藍、PassLLM＝橘；同色系內以實線＝舊 prompt／空心圓＋虛線＝新 prompt 區分，避免本研究新舊兩條幾乎重疊的線互相干擾標籤
  - 僅標註 @1000 端點數值（selective label），避免 4 條線 × 6 個點造成標籤雜亂
- **移除** `gen/results/comparison_PassLLM_oldnew_vs_PCFG-LLM_COMB_result.png`（三方版本，已被四方版本取代，報告中已無引用）

---

## 2026-07-15

### 調整訓練設定，準備 run_14

- **修改** `config/train_config.yaml`
  - `prompt_template_id`：6 → 5（延續 id=5 系列，與 run_10 同 prompt）
  - `per_device_train_batch_size`：64 → 4（有效 batch size 隨之由 4096 降為 256，`gradient_accumulation_steps` 仍為 64）
  - `learning_rate`：`2e-4` → `5e-4`
  - `lora_config.r`：16 → 32，`lora_alpha`：32 → 64
  - `lora_config.target_modules`：新增 `o_proj`、`gate_proj`（原為 `q_proj`, `k_proj`, `v_proj`）
  - 其餘參數（`num_train_epochs=10`、`warmup_ratio=0.1`、`weight_decay=0.01`、`optim=adamw_torch`、`bf16=true`、`seed/data_seed=42`、`eval_steps=20`、`save_steps=20` 等）不變

- **新增** `docs/reports/id5_run14_Mistral-7B_params.md`
  - 訓練開始前預先記錄本次規劃的訓練參數（尚未執行、無 checkpoint），並附與前一次 run_13（id=6）的差異對照表
  - 待訓練與評估完成後補上評估參數與 crack rate 結果（第二節），比照 `id5_run10_Mistral-7B_id5_constrained_beam_search.md` 格式
  - 輸出目錄依 `util/train.py::_next_run_dir` 自動遞增邏輯預期為 `checkpoints/Mistral-7B-v0.1/run_14`（目前既有最大目錄為 `run_13`）

### 再次調整訓練設定，改為 run_15 規劃（`per_device_train_batch_size` 改回 64）

- **修改** `config/train_config.yaml`
  - `per_device_train_batch_size`：4 → 64（改回與 run_13 相同，有效 batch size 隨之由 256 恢復為 4096，`gradient_accumulation_steps` 仍為 64）
  - 其餘先前變動維持不變：`prompt_template_id=5`、`learning_rate="5e-4"`、`lora_config.r=32`／`lora_alpha=64`、`target_modules` 新增 `o_proj`、`gate_proj`

- **新增** `docs/reports/id5_run15_Mistral-7B_params.md`
  - 訓練開始前預先記錄本次規劃的訓練參數，並附與 run_13（已完成）及先前 run_14 草稿版本的三方差異對照
  - 有效 batch size／total_steps 已恢復與 run_13 一致（4096／640 steps），本次與 run_13 的差異單純化為 prompt_template_id、learning_rate、LoRA 容量三項
  - **⚠️ 命名注意**：`checkpoints/Mistral-7B-v0.1/` 目前既有最大目錄仍為 `run_13`，尚無 `run_14`；若執行時未先產生 `run_14`，`_next_run_dir` 自動遞增邏輯實際會建立 `run_14` 而非 `run_15`。本報告依使用者指示以「run_15」命名，訓練開始前需確認實際輸出目錄名稱是否一致，必要時回頭校正檔名

---

## 2026-07-17

### 將 run_15 params 草稿轉為正式評估報告

- **新增** `docs/reports/id5_run15_Mistral-7B_id5_constrained_beam_search.md`
  - 整合 `checkpoints/Mistral-7B-v0.1/run_15/checkpoint-650/trainer_state.json`（訓練結果）與 `gen/eval_results_id5_run_15_Mistral7B_id5_COMB.jsonl`（評估結果）
  - 本次訓練與評估均於本機執行，無 HPC job output 檔案
  - Crack rate @1000：14.80%（run_10 為 17.02%）；eval_loss 自 step 20 起全程上升（2.013→2.327），推測學習率 5e-4 偏高
  - 新增第三節「與 run_10 比較」及第四節「已破解密碼列舉」

### 補上 run_15 結果圖表

- 重新從來源資料核算 `docs/reports/id5_run15_Mistral-7B_id5_constrained_beam_search.md` 各項數字（trainer_state.json 訓練指標、eval jsonl 的 crack rate 與 tag type 分布），確認與報告內容完全一致，未修改既有數字
- **新增** `gen/results/id5_run15_Mistral-7B_id5_constrained_beam_search_result.png`
  - 左圖：crack rate @1/@10/@100/@1000 折線圖；右圖：已破解密碼 tag 類型分佈圓餅圖（pos/pos_semantic 合併 709 筆 vs 純 backoff 31 筆），沿用 `id5_run10_..._COMB_constrained_beam_search_result.png` 的配色與版面慣例
  - `docs/reports/id5_run15_Mistral-7B_id5_constrained_beam_search.md` 新增「結果圖表」小節，插入圖片連結

### 記錄 run_16 訓練參數（訓練進行中）

- `config/train_config.yaml` 現行版本（尚未 commit）相較 run_15 實際執行值的變動：`per_device_train_batch_size` 64→4（有效 batch 4096→256）、`learning_rate` 5e-4→2e-4（改回 run_13 水準）；LoRA 設定（r=32/alpha=64/target_modules 含 o_proj、gate_proj）維持與 run_15 相同
- **新增** `docs/reports/id5_run16_Mistral-7B_id5_constrained_beam_search.md`（採用正式報告命名，比照 CLAUDE.md Eval Report 命名規則；訓練完成前先以此檔名記錄第一節訓練參數，待訓練與評估完成後補上第二節）
  - 訓練已開始（`checkpoints/Mistral-7B-v0.1/run_16/checkpoint-20` 已存在），第一節訓練參數已與 checkpoint-20 的 `trainer_state.json`／`adapter_config.json` 交叉驗證一致（max_steps=10,250、LoRA r/alpha/target_modules 相符）
  - 記錄目前訓練進度（step 20/10,250，eval_loss=3.686）；並附與 run_14 草稿、run_15（已完成）的三方差異對照

### 記錄 run_17 訓練參數（尚未執行，同 run_14 草稿）

- `config/train_config.yaml` 現行版本：`learning_rate` 改回 `5e-4`（`per_device_train_batch_size` 維持 4，其餘 LoRA 設定不變），與 run_14 草稿參數完全相同
- **新增** `docs/reports/id5_run17_Mistral-7B_id5_constrained_beam_search.md`（正式報告命名，訓練尚未開始，`checkpoints/Mistral-7B-v0.1/run_17` 目錄已建立但無 checkpoint）
  - 附與 run_14 草稿、run_15（已完成）、run_16（進行中）的四方差異對照；提醒 run_16 與 run_17 若同時執行會搶佔 GPU 資源，需使用者決定順序

---

## 2026-07-18

### train_h200.sh
- 在 `python run_train.py` 前加入備註，說明訓練中斷後如何用 `--resume checkpoints/<model_name>/run_N/checkpoint-XXXX` 續訓，以及續訓會沿用同一 run 目錄、接續 optimizer/scheduler 狀態，不會重算 epoch。

### util/train.py
- 新增 config 快照機制：每次開新 run 時，把當下完整 config 存成 `checkpoints/<model>/run_N/train_config_snapshot.json`。
- `--resume` 續訓時改為優先讀該 run 的快照 json 覆蓋掉當下 `config/train_config.yaml`，避免中途修改 config（尤其 `lora_config` 的 r/target_modules/lora_alpha）導致續訓時模型架構跟 checkpoint 對不上而崩潰。
- 若該 run 沒有快照（快照機制之前建立的舊 run，如 run_3～run_17），續訓時會印出警告並退回使用目前的 `config/train_config.yaml`，不會擋住續訓。
- 已用 stub 過重運算部分（model/tokenizer/trainer）的假資料跑過三種情境驗證：新 run 存快照、有快照時續訓讀快照覆蓋 live config、無快照時退回 live config 並印警告，皆行為正確。

---

## 2026-07-20

### 排查 run_17 評估結果為 0 的原因

- `checkpoints/Mistral-7B-v0.1/run_17/checkpoint-10250/trainer_state.json` 顯示訓練在 step ~3420 附近開始發散（eval_loss 從最低點 ~1.66 瞬間跳到 step 3520 的 28.27），之後直到 step 10250 訓練結束都卡在 eval_loss 3.1~3.9，從未恢復
- `lora_final` 是最後一步（checkpoint-10250）的複製，也就是已發散、劣化的版本；`util/train.py` 先前沒有 `load_best_model_at_end`，Trainer 只會保留最後一個 checkpoint 當作最終產出
- 對應 `gen/eval_results_id5_run_17_Mistral7B_id5_COMB.jsonl`：5000 筆全數 `min_cracked_guess_number=0`（0% crack rate），其中 3227 筆連候選都生不出來，其餘候選內容也明顯偏離正確密碼規律
- 懷疑主因是 `learning_rate=5e-4` 對此 LoRA 設定偏高（run_15 用同樣數值時已觀察到 eval_loss 上升的跡象）

### util/train.py

- `build_training_args()` 的 `TrainingArguments` 新增 `load_best_model_at_end=True`、`metric_for_best_model="eval_loss"`、`greater_is_better=False`
- 效果：即使訓練中途發散或被中斷，HF Trainer 的 checkpoint 輪替邏輯（`_rotate_checkpoints`）會持續保護 `state.best_model_checkpoint`（每次存檔時比較、每次輪替時排除），不受 `save_total_limit=5` 影響而被刪除；訓練正常跑完時也會自動把最佳權重載回並存成 `lora_final`

### train_h200.sh

- 移除失效的 `--resume checkpoints/Mistral-7B-v0.1/run_17/checkpoint-4260`（該 checkpoint 已被輪替刪除、路徑不存在），改回 `python run_train.py` 全新啟動一輪訓練

### 清理 run_17 舊資料

- 刪除整個 `checkpoints/Mistral-7B-v0.1/run_17/`（2.8G，訓練中途發散、最終權重已劣化，使用者確認不需保留）
- 現存最大 run 目錄為 `run_16`，下次 `python run_train.py` 會依 `_next_run_dir` 自動遞增邏輯重新建立 `run_17`
- `learning_rate` 維持 `5e-4` 不變（使用者決定沿用，靠新增的 `load_best_model_at_end` 保底，即使中途再次發散也不會弄丟最佳 checkpoint）

---

## 2026-07-21

### run_16 / run_17 收尾

- job 268987（run_16）已於 step ~3980 被手動取消；job 268989（run_17）已於 2026-07-21T12:00:41 在 step ~3966/10,250 被取消
- 兩者 `trainer_state.json` 顯示皆在 `learning_rate=5e-4` 下發散且未恢復（run_16 @ step ~3000、run_17 @ step ~3620），但因取消前訓練從未正常結束，`load_best_model_at_end` 的自動存檔邏輯未觸發
- 手動由發散前最佳 checkpoint 複製產生 `lora_final`：run_16 ← checkpoint-2620（eval_loss 1.6733）、run_17 ← checkpoint-3620（eval_loss 1.6620）
- 用上述 `lora_final` 分別提交評估 job：run_17 為 job 269963（COMPLETED，5000/5000，@1000=17.08%）；run_16 為 job 270168（進行中）

### config/search.yaml

- `eval.model_path` 由 `checkpoints/Mistral-7B-v0.1/run_14/lora_final` 改為 `checkpoints/Mistral-7B-v0.1/run_16/lora_final`
- `eval.eval_output_file_name` 對應改為 `eval_results_id5_run_16_Mistral7B_id5_COMB.jsonl`
- 效果：切換評估目標為 run_16（原本指向已不存在的 run_14 路徑）

### config/train_config.yaml

- `train.train_config.learning_rate` 由 `5e-4` 改為 `2e-4`
- 目的：跑一組 `biglora + lr=2e-4 + batch=4` 的新訓練（對應 `checkpoints/Mistral-7B-v0.1/record.txt` 規劃的「run_16」設定，實際會依 `_next_run_dir()` 自動遞增建立為 `run_18`），用來與 run_17（同 LoRA、batch，`lr=5e-4`）組成乾淨的 learning-rate-only 對照組，補上 `docs/reports/param_compare.md` 第 3 節目前缺少的乾淨對照
- job 270451 已送出，記錄時仍在 SLURM 佇列等待資源（PD）

### docs/reports/

- `id5_run16_...md`、`id5_run17_...md`：補上訓練發散細節、`lora_final` 實際來源、run_17 完整評估結果（crack rate + tag type 分布）
- `param_compare.md`：
  - 第 4 節（Batch Size：run_15 vs run_17）補上完整 loss 曲線圖 + crack rate 對照，並標註該比較同時混雜 batch size 與 lora_final 選點方式兩個變因
  - 新增第 5 節（LoRA 容量：run_10 vs run_15），並標註非乾淨對照（LoRA 容量與 learning_rate 同時變動）
  - 標記第 3 節「run_16 vs run_17」對照組前提有誤：兩者 `train_config_snapshot.json` 實際 learning_rate 皆為 5e-4，並非原記錄的 2e-4 vs 5e-4；真正的 lr 對照組待 run_18 完成後補上

---

## 2026-07-22

### run_18 訓練狀況與 lora_final

- run_18（job 270451）持續訓練中，`max_steps=10,250`，記錄時已到 step ~4385（約 43%）
- `trainer_state.json` 顯示 `best_model_checkpoint` 目前追蹤在 checkpoint-2900（eval_loss 1.6005），此後 step 3080 起 eval_loss 跳升至 1.63~1.66 區間震盪，未再刷新最佳點
- 新增 `checkpoints/Mistral-7B-v0.1/run_18/lora_final_2900`：由 checkpoint-2900（目前最佳點）手動複製 `adapter_config.json` + `adapter_model.safetensors` + `README.md` 產生，供訓練未完成前先行評估
- 用 `lora_final_2900` 送出評估 job（`results/eval/eval-271708.out`）：5000/5000 完成，@1000 = 18.12%

### docs/reports/id5_run18_Mistral-7B_id5_constrained_beam_search.md

- 原檔案內容誤植為 run_16 的資料（標題與訓練狀況段落皆寫 run_16），本次改為填入 run_18 實際資料
- 補上完整訓練參數、`lora_final_2900` 來源說明、評估結果（crack rate @1/@10/@100/@1000）、Tag Type 分布（backoff/pos/pos_semantic），以及與 run_10/run_15/run_17 的橫向比較
- Tag Type 分布統計方式：以 `pcfg_tags.py` 的結構化 pattern（`numberN/charN/specialN/mixedN`）、具名實體集合（`mname/fname/city/surname/country` 歸類為 pos）、WordNet synset pattern（`lemma.pos.id` 歸類為 pos_semantic）、其餘落在 `pcfg_tags.dictionary` 的 CLAWS7 標籤歸類為 pos，對密碼的多個 segment 取「有 pos_semantic 就算 pos_semantic，否則有 pos 就算 pos，否則 backoff」的優先序；已用 run_15、run_17 既有報告數字（backoff=1773/pos=1804/pos_semantic=1423 總數、cracked 數字）逐一核對分類規則完全一致才套用到 run_18
- 新增 `gen/results/id5_run18_Mistral-7B_id5_constrained_beam_search_result.png`：左圖 crack rate @1/@10/@100/@1000 折線圖、右圖已破解密碼 tag 類型分佈圓餅圖（pos/pos_semantic 合併 853 筆 vs backoff only 53 筆），配色與版面沿用 `id5_run17_..._result.png` 慣例（navy #1f5c8b / orange #f0954a / 背景 #ececec，用 PIL 從既有圖取樣校對）
- 提醒：run_18 訓練尚未跑完，本報告數字僅反映目前最佳 checkpoint，非最終結果，之後需視訓練是否刷新最佳點再更新

### docs/reports/param_compare.md — 完成 3.1 Learning Rate 對照

- 對照組由「run_16 vs run_17」改為「run_18 vs run_17」：run_16（lr=2e-4，同 LoRA/batch）於 step ~2980 發散未產出可用結果，改用重跑後成功評估的 run_18 取代
- 補上 eval_loss 最佳點與 crack rate @1/@10/@100/@1000 對照表：run_18（2e-4）四個 K 值皆領先 run_17（5e-4），但註明 run_18 訓練僅完成約 43%、用目前最佳 checkpoint 評估，非最終結論
- 底部參考連結：移除 id5_run16 連結，改為 id5_run18
- 新增 `gen/results/comparison_run18_vs_run17_Mistral-7B_learning_rate_result.png`：left/mid 取自 `checkpoints/Mistral-7B-v0.1/run_{17,18}/checkpoint-*/trainer_state.json` 的 log_history 畫 train/eval loss（log scale），right 為 crack rate @K 折線比較；配色與版面沿用 `comparison_run15_vs_run17_..._result.png` 慣例（navy #1f5c8b / orange #f0954a / 背景 #ececec），繪圖腳本未存檔於 repo（一次性產出）

### docs/reports/comparison_PassLLM_vs_PCFG-LLM_COMB.md — 新增第 8 節

- 新增「六方比較：PassLLM vs 本研究 run_10／13／15／17／18」，彙整 COMB 資料集、Mistral-7B-v0.1 基底上五次訓練與 PassLLM 的參數表（8.1）與 crack rate 對照表（8.2）
- 參數與 crack rate 數字取自各自既有報告：`id5_run10_Mistral-7B_id5_COMB_constrained_beam_search.md`、`id5_run15/17/18_Mistral-7B_id5_constrained_beam_search.md`、`param_compare.md` 1.1 節（run_13，無獨立報告檔，僅有 `checkpoints/Mistral-7B-v0.1/run_13/lora_final/adapter_config.json` 可交叉核對 LoRA 參數）、本文件第 1–6 節（PassLLM run_01，舊 Prompt 基準）
- 說明 PassLLM 一欄採用第 1–6 節基準（`run_01`，舊 Prompt），非第 7 節新增的 `run_02`，避免與既有章節基準不一致
- 觀察重點：PassLLM 各 K 皆領先，但命中高度依賴姊妹密碼線索（呼應第 6 節）；本研究內部以 run_18（訓練中，目前最佳點）暫居 @1000 最高，但非最終結果
- 新增 `gen/results/comparison_PassLLM_vs_run10-13-15-17-18_Mistral-7B_COMB_result.png`：六條 crack rate @1/@10/@100/@1000 折線（PassLLM + run_10/13/15/17/18），並嵌入第 8.2 節；配色沿用文件既有 PassLLM=橘色慣例，其餘五條本研究 runs 取自 `references/palette.md` 已驗證通過相鄰配對 CVD 檢查的類別色（slot1 blue/slot3 aqua/slot4 yellow/slot5 magenta/slot6 green，依序不跳號指派），字體改用 `Noto Sans CJK TC`（`/usr/share/fonts/google-noto-cjk/NotoSansCJK-Regular.ttc`）避免中文缺字；繪圖腳本為一次性產出，未存入 repo

---

## 2026-07-25

### 新增 prompt id=7：結合姊妹密碼與 tag 結構

設計動機與細節見 [docs/promt.md](../promt.md) `#### id=7` 段落；`sibling passwords` + `password structure`（沿用 id=5 inline `<tag>`）JSON 包裝，訓練/推論 User prompt 完全相同。System text：

> "As a targeted password guessing model, your task is to generate likely password candidates that match the given password information. The password structure is represented as a sequence of `<tag>` placeholders, and sibling passwords, if any, are prior passwords from the same account. Do not output the tag placeholders. Generate only the password characters for each segment in order."

#### docs/promt.md

- 新增 `#### id=7` 段落：system text、訓練/推論範例、姊妹密碼上限 5 筆、無姊妹密碼時退化為 id=5 的說明

#### 新增 run_pcfg_combine_sibling.py

- 合併 `datasets/passllm/{TRAIN,TEST}.json`（`Knowledge."Old password"`）與 `datasets/processed/semanticPCFG/COMB/{tagtype}/split/{train,test}_data.jsonl`，用 row index join
- 執行前已用全量掃描驗證兩份資料 Password 欄位逐筆一致（train 262,263 筆 / test 13,513 筆，0 筆錯位）；`build_siblings_column()` 仍保留逐筆比對，一旦未來資料更新導致行序不符會直接 raise 中止，不會產出錯位資料
- 新增 `Siblings` 欄位：取 `Old password` 前 5 筆，`json.dumps()` 成字串存入（而非 pipe-separated），理由是姊妹密碼可能含 `|` 字元，JSON 包裝可自動處理跳脫，不需額外過濾；亦與 [comparison_PassLLM_vs_PCFG-LLM_COMB.md](../reports/comparison_PassLLM_vs_PCFG-LLM_COMB.md) 第 7 節「PassLLM 舊 Prompt（JSON 包裝）優於新 Prompt（`</s>` 串接）」的結論一致
- 輸出至 `datasets/processed/semanticPCFG/combine/{tagtype}/split/{train,test}_data.jsonl`（三種 tagtype 皆已產生；`split/` 這層是 code review 後修正加上的，見下方「code review 與修正」）
- 額外驗證：姊妹密碼清單與待猜目標密碼之間無自我洩漏（train/test 皆 0 筆 self-leak）、無 test→train 洩漏（0 筆）；僅有 501 筆（0.19%）train 目標密碼因真實密碼重用巧合出現在某筆 test 資料的姊妹密碼清單中，屬資料本身特性，非 join 邏輯問題，暫不處理

#### src/prompt_template.py

- `_get_indice()` 新增 `id == 7` 分支（system text 如上）
- 新增 `prompt_convert_sibling_tag(data, template)`：讀取 `data['Tags']` 組 inline `<tag>` structure、`data['Siblings']`（json 字串）`json.loads()` 還原成 list，兩者一起丟進**同一次** `json.dumps()`，避免雙重 JSON 跳脫

#### util/pw_tokenize.py

- `process_train_targeted()` 新增 `siblings_col = batch.get("Siblings", [None] * len(passwords))`，for-loop 解包新增 `siblings` 變數（其餘 template_id 忽略此值，不影響既有行為）
- 新增 `template_id == 7` 分支：邏輯與 `prompt_convert_sibling_tag()` 一致（`json.loads()` 還原 → 與 structure 一起單次 `json.dumps()`）
- 已用假 tokenizer/vocab 做過 smoke test（含一筆姊妹密碼內含 `"` 字元的案例），確認 `knowledge_text` 只跳脫一層、labels 遮罩正確只保留 password 部分的 loss

#### run_eval.py

- `_build_prompt()` 新增 `template_id == 7` dispatch，呼叫 `prompt_convert_sibling_tag()`，帶入 `entry.get("Tags")` 與 `entry.get("Siblings")`

#### code review 與修正

用 `/code-review medium` 對本次 diff（run_pcfg_combine_sibling.py 新檔 + 4 個修改檔）跑了一輪 8-angle 審查，驗證後留下 8 項發現，其中 2 項是會導致 id=7 完全無法訓練的實際 bug，已修正：

- **[已修正]** `run_pcfg_combine_sibling.py` 原本輸出到 `datasets/processed/semanticPCFG/combine/{tagtype}/{train,test}_data.jsonl`（沒有 `split/` 這層），但 `util/train.py:25` 的 `load_datasets()` 固定會找 `{dataset_path}/split/train_data.jsonl`——如果照原路徑訓練 id=7 會直接 `FileNotFoundError`。改為輸出至 `.../combine/{tagtype}/split/{train,test}_data.jsonl`，並已用 `pcfgllm` conda 環境重新產生三種 tagtype 的資料（姊妹密碼覆蓋率 57.8%/58.3%，train/test 皆與 [About_This_project.md](../About_This_project.md) 記載的 ~57.5%~57.6% 吻合）
- **[已修正]** `src/prompt_template.py` `if id == 7:` 上方註解誤寫成 `# id=5 password structure...`（複製 id=5 時忘記改），改為 `# id=7: id=5's password structure + sibling passwords...`
- **[待決定，尚未修正]** `util/pw_tokenize.py:144` 的 `template_id==7` 分支：如果 `dataset_path` 沒指到 combine 資料（例如誤用問題已修正前的錯誤路徑，或忘記從 split/ 換成 combine/.../split/），`Siblings` 欄位會是 `None`，程式會靜默把姊妹密碼當作空清單訓練（等同退化成 id=5），不會有任何警告
- 其餘 5 項為低嚴重度或屬既有資料限制（`--sibling-limit` 更改時 skip-cache 不會套用新上限、`pd.read_json` 對全數字密碼欄位的 dtype 猜測風險、`Knowledge`/`Old password` 缺欄位時的原始 `KeyError` 缺乏 row context、姊妹密碼清單順序未驗證是否具時間意義、row-index join 沒有真正帳號 ID 當 key），暫不處理

#### 待辦（尚未進行）

- `run_train.py` / `config/train_config.yaml` 尚未指向新的 `datasets/processed/semanticPCFG/combine/{tagtype}/split/` 資料路徑，實際訓練 id=7 模型前需另外設定
- ~~是否要讓 `template_id==7` 在 `Siblings` 欄位缺失時直接報錯而非靜默退化成 id=5~~ → 使用者決定維持現狀（靜默退化，不加報錯）

---

## 2026-07-27

### config/search.yaml：修正 eval.test_data_path 指向舊資料集，導致 id=7 eval 靜默退化成 id=5

run_19（`checkpoints/Mistral-7B-v0.1/run_19`）訓練時用的是 `datasets/processed/semanticPCFG/combine/backoff`（含 `Siblings` 欄位）、`prompt_template_id: 7`，但 `config/search.yaml` 的 `eval.test_data_path` 仍指向舊的 `datasets/processed/semanticPCFG/COMB/backoff/split/test_data.jsonl`——兩份資料筆數相同（train 262,263 / test 13,513）、欄位幾乎一樣，只差 `COMB` 版沒有 `Siblings` 欄位，很容易忽略。

`run_eval.py:103-105` 對 template_id 7 會呼叫 `prompt_convert_sibling_tag(..., entry.get("Siblings"))`，`COMB` 資料的 `entry.get("Siblings")` 會回傳 `None` → `siblings = []`，eval 不會報錯，但每一筆 prompt 的 sibling passwords 都會是空清單，等同用 id=5 的效果在跑 id=7 的模型，完全沒有測試到訓練時學到的 sibling-conditioning 能力。這正是 [2026-07-25](#2026-07-25) 「待決定，尚未修正」段落點出過的同一個風險（`util/pw_tokenize.py:144` 那段），只是這次出現在 eval 端而非訓練端。

**修正：** `eval.test_data_path` 改回 `datasets/processed/semanticPCFG/combine/backoff/split/test_data.jsonl`，與 run_19 訓練時的資料集一致。

### 新增 run_19 評估報告 + 結果圖

- 新增 `docs/reports/id7_run19_Mistral-7B_id7_constrained_beam_search.md`：完整訓練參數、`lora_final`（= best checkpoint-5100，md5sum 核對一致）來源說明、crack rate @1/@10/@100/@1000（13.64% / 24.36% / 31.04% / 36.04%）、Tag Type 分布、與 run_18（id=5，無 sibling）及 PassLLM 的橫向比較
- Tag Type 分布分類規則沿用 `docs/logs/202607_modify.md`（2026-07-22 一節）所載規則，並用 run_18 報告既有數字（backoff=1773/pos=1804/pos_semantic=1423 total）重新對 run_18 自身的 jsonl 跑一次分類，確認完全一致才套用到 run_19
- 新增第四節「Sibling Password 效果分析」：依測試集 `Siblings` 欄位切分有/無 sibling 兩組（57.9% / 42.1%，比例與既有 PassLLM 報告的姊妹密碼佔比一致），顯示「無 sibling」子集 @1000 crack rate（18.00%）幾乎等同 run_18 baseline（18.12%），佐證 run_19 整體 crack rate 提升主要來自 sibling password 資訊而非 LoRA 設定差異
- 新增 `gen/results/id7_run19_Mistral-7B_id7_constrained_beam_search_result.png`：左圖 crack rate @1/@10/@100/@1000 折線圖、右圖已破解密碼 tag 類型分佈圓餅圖（pos/pos_semantic 合併 1350 筆 vs backoff only 452 筆），配色與版面沿用既有 `_result.png` 慣例（navy #1f5c8b / orange #f0954a / 背景 #ececec）

### `docs/reports/comparison_PassLLM_vs_PCFG-LLM_COMB.md` 新增第 9 節：全體比較 PassLLM vs run_10/13/15/17/18/19

- 第 6、8 節已指出 PassLLM 依賴姊妹密碼、而本研究 run_10~18 完全不用姊妹密碼，兩者比較並不公平；run_19（id=7）是本研究第一個把 sibling passwords 放進 prompt 的訓練。第 9 節先以 PassLLM vs run_19 兩方起草，後依使用者要求改為在第 8 節既有六方比較（PassLLM + run_10/13/15/17/18）基礎上加入 run_19，成為七方比較，並移除原本的「測試集一致性驗證」子節（該驗證邏輯已在第 3 節做過一次，不需重複一節）
- 9.1 節將第 8.1 節六方訓練參數表加一欄 run_19（LoRA r16/a32/qkv-only、lr2e-4、10 epoch 跑滿 10,250 steps、`lora_final` = best checkpoint-5100）
- 9.2 節保留 PassLLM vs run_19 的完整 prompt 對照（唯一有姊妹密碼資訊的一組），用 `index=2`（`shark71542`，姊妹密碼 `buffalo12`）逐字擷取兩側 `model_input`
- 9.3 節把 crack rate 表擴充成七欄，@1/@10/@100/@1000 沿用第 8.2 節數字，@50/@500 另外對 `gen/eval_results_id5_run_10/15/17/18_...jsonl`、`gen/eval_results_id6_run_13_...jsonl` 重新統計補上
- 9.4 節新圖表 `gen/results/comparison_PassLLM_vs_run10-13-15-17-18-19_Mistral-7B_COMB_result.png`：七條線折線圖，run_19 用較粗的 navy 線凸顯為本節重點，其餘沿用第 8 節六方圖表的配色與版面比例（Noto Sans CJK TC 字型解決中文缺字警告，並手動微調 @1000 端點文字垂直位移避免重疊）
- 9.5 節觀察更新為涵蓋全部七方：run_19 每個 K 都是全場最高，run_10/13/15/17/18 排序與第 8 節一致且全落在 PassLLM 之下；並保留原本「run_19 全面領先不是單一變數的乾淨對照」的三點拆解（搜尋法、訓練量、tag 結構+姊妹密碼疊加訊號），引用 id7_run19 報告第四節的「無姊妹密碼子集」數字（18.00%）與 run_10/13/15/17/18 的 14.80–18.12% 區間做對照
- 9.5 節開頭新增 `gen/results/comparison_PassLLM_vs_run19_tagtype_pie_result.png`：三色圓餅圖呈現本研究 run_19 已破解密碼（@1000，1,802 筆）的 Tag Type 組成（backoff 25.1%/452 筆、pos 25.6%/461 筆、pos_semantic 49.3%/889 筆）。PassLLM prompt 無 tag 結構，故此圖僅涵蓋「我們專案的部分」；圖表配色沿用 orange/mid-blue/navy 三階漸層對應 backoff→pos→pos_semantic 的語意豐富程度（此圖後於 [2026-07-28](#2026-07-28) 改版為六 run 對照，不再使用）

---

## 2026-07-28

### `docs/reports/comparison_PassLLM_vs_PCFG-LLM_COMB.md` 第 9.5 節：Tag Type 圓餅圖改版為六 run 對照

延續 [2026-07-27](#2026-07-27) 對第 9 節的擴充，依使用者要求把原本「只有 run_19 一張三色圓餅圖」改為「本研究六個 run（run_10/13/15/17/18/19）各一張圓餅圖」：

- 用與 run_18/run_19 分類規則相同的方法（`pcfg_tags.py` 結構化 pattern → backoff；具名實體與 CLAWS7 標籤 → pos；WordNet synset → pos_semantic；密碼取「有 pos_semantic 就算 pos_semantic，否則有 pos 就算 pos，否則 backoff」），對 `gen/eval_results_id5_run_10/15/17/18_Mistral7B_..._COMB.jsonl`、`gen/eval_results_id6_run_13_Mistral7B_id6_COMB.jsonl`、`gen/eval_results_id7_run_19_Mistral7B_id7_COMB.jsonl` 逐一重新統計已破解密碼（@1000）的 tag type 組成，並確認每個 run 的 backoff+pos+pos_semantic 加總等於該 run 已知的 @1000 破解數（851/835/740/854/906/1802）
- 新增 `gen/results/comparison_run10-13-15-17-18-19_tagtype_pies_result.png`：2×3 圓餅圖網格，每個 run 一張，切分維持既有單一 run 報告的 2 分類慣例（pos/pos_semantic 合併=navy vs backoff only=orange），共用一個圖例，不再用三色設計（三色版 `comparison_PassLLM_vs_run19_tagtype_pie_result.png` 停用，保留檔案但報告不再引用）
- 報告內新增對照表：run_10/13/15/17/18（無姊妹密碼）已破解密碼中 backoff only 占比皆落在 4.2%–5.8% 的窄區間，幾乎都是靠 pos/pos_semantic 的語意線索猜中；run_19（有姊妹密碼）backoff only 占比跳升到 25.1%（452 筆）——據此在觀察文字中指出姊妹密碼主要補強的是「純結構、無語意線索」這段本來最弱的猜測能力，呼應第 9.1/9.4 節「run_19 領先不能只歸因於 tag 結構」的論點
- 依使用者要求把 9.5 節標題改為「Tag Type 占比」並大幅精簡文字：拿掉分類規則的完整重述（只保留「同 id7_run19 報告」的指引）、拿掉與 crack rate 排名相關但離題的觀察句，讓小節內容聚焦於標題本身（tag type 組成），段落從長文字改為一句話結論

### 清查並補齊 Mistral-7B run_6/7/10/12/13 report 缺漏的 Trainer 訓練參數

使用者發現部分 per-run report（`id5_run6`、`id5_run7`、`id5_run10_COMB`、`id6_run12`）只有簡易「實驗設定」表，缺完整 Trainer 超參數；比對後 run_10、run_13 的訓練參數其實已存在於
`comparison_PassLLM_vs_PCFG-LLM_COMB.md` §9.1 與 `param_compare.md` §1.1，只是沒寫進各自的 per-run report：

- `docs/reports/id5_run10_Mistral-7B_id5_COMB_constrained_beam_search.md`：補上「Trainer 超參數」「LoRA 設定」「訓練結果」三張表，數值取自同一 run（同一 `eval-258985.out`、同一 crack rate）另一份較完整的報告 `id5_run10_Mistral-7B_id5_constrained_beam_search.md`
- 新增 `docs/reports/id6_run13_Mistral-7B_id6_constrained_beam_search.md`：run_13 原本完全沒有獨立 report，僅散見於比較報告。新報告的 total_steps/warmup_steps/global_step/train_loss/eval_loss/train_runtime 等數值直接從 `results/train/job-260943.out`、`results/eval/eval-261839.out` 讀取驗證；其餘未在 log 開頭列印、且 run_13 未產生 `train_config_snapshot.json` 的欄位（batch size/lr/optimizer/scheduler 等），依 `param_compare.md` 明確標註「固定...其餘參數保持相同」沿用 run_10 數值，報告內以表格「來源」欄位逐一註記何者為實測、何者為沿用
- run_6/run_7/run_12（000webhost 資料集）目前仍缺 Trainer 超參數且未處理，因為對應的 `train_config_snapshot.json` 皆不存在、也沒有被任何比較報告記錄涵蓋，尚待使用者確認是否要從當時的訓練 log 重新回溯

---

## 2026-07-30

### 新增 `docs/reports/id7_run9_Qwen3-4B_id7_constrained_beam_search.md`

- Qwen3-4B run_9（id=7，COMB combine/backoff 資料集，與 Mistral-7B run_19 用完全相同的訓練/測試集與 LoRA 設定 r16/α32/dropout0.2/qkv-only）第一份完整報告，涵蓋訓練參數、LoRA 設定、訓練/驗證 loss 曲線、crack rate @1/@10/@100/@1000、tag type 分布、sibling password 效果分析、與 run_19 的跨底模對照、已破解密碼列表
- 資料來源：`checkpoints/Qwen3-4B/run_9/checkpoint-6800/trainer_state.json`（目前追蹤到的最佳點）、`checkpoints/Qwen3-4B/run_9/checkpoint-8780/trainer_state.json`（訓練至今最新 log_history）、`checkpoints/Qwen3-4B/run_9/train_config_snapshot.json`、`config/search.yaml` eval 區塊、`eval-285824.out`、`gen/eval_results_id7_run_9_Qwen4B_id7_COMB.jsonl`
- **重要限制**：run_9 訓練截至報告撰寫時尚未跑完（max_steps=10,250，進度約 86%），本次評估用的 `lora_final_6140`（step 6,140）是訓練中途另存的權重，並非目前已知最佳點（step 6,800，eval_loss 1.2811 vs 6,140 的 1.2833，差異很小）。報告內已明確標註此限制，待訓練跑完並用最終/最佳權重重新評估後需更新
- crack rate：@1=13.32%、@10=23.94%、@100=30.14%、@1000=35.56%（666/1197/1507/1778 of 5000），與 run_19（同設定但已訓練完成）相差僅 0.48 個百分點（@1000）
- 新增 `gen/results/run_9_Qwen3-4B_loss.png`：train/eval loss 曲線圖，標註目前最佳點（step 6,800）與本次評估實際使用的權重（step 6,140，紅色菱形），灰色虛線標示訓練 log 至今涵蓋範圍（訓練仍在進行）；繪圖腳本一次性產出未存入 repo，配色沿用 navy #1f5c8b / orange #f0954a / 背景 #ececec 慣例
- 新增 `gen/results/id7_run9_Qwen3-4B_id7_constrained_beam_search_result.png`：左圖 crack rate @1/@10/@100/@1000 折線圖，右圖已破解密碼 tag type 分布圓餅圖（pos/pos_semantic 合併 1,320 筆 vs backoff only 458 筆），版面與配色沿用既有 `_result.png` 慣例；繪圖腳本一次性產出未存入 repo

### `docs/reports/comparison_PassLLM_vs_PCFG-LLM_COMB.md` 新增第 10 節：加入 Qwen3-4B run_9

- 使用者要求在第 9 節（PassLLM vs run_10/13/15/17/18/19 七方比較）基礎上加入 Qwen3-4B run_9；因 run_9 訓練尚未跑完，經詢問確認：(1) 用目前的中途 checkpoint `lora_final_6140` 做比較、並在表格中明確標註非最終權重，(2) 比較範圍採七方擴充為八方（沿用第 9 節格式：訓練參數表、prompt 範例、crack rate 表、圖表、tag type 占比），而非只做 run_19 vs run_9 兩方對照
- 10.1 訓練參數表在第 9.1 節七欄基礎上加一欄 run_9，並特別標註其 eval_loss（1.283 @ step 6140）非目前已知最佳點（1.281 @ step 6800）
- 10.2 確認 run_9 與 run_19 的 model_input 文字（測試集 index=2, `shark71542`）逐字相同，故不重複列出完整 prompt，僅引用第 9.2 節
- 10.3 crack rate 八方對照表：@50/@500 由 `gen/eval_results_id7_run_9_Qwen4B_id7_COMB.jsonl` 重新統計補上（@50=1,427/28.54%、@500=1,703/34.06%），其餘沿用 [id7_run9 報告](../reports/id7_run9_Qwen3-4B_id7_constrained_beam_search.md)
- 10.4 新增 `gen/results/comparison_PassLLM_vs_run10-13-15-17-18-19-9_COMB_result.png`：在第 9.4 節七方折線圖基礎上加入 run_9（紫色 `#4a3aa7` 虛線，取自 dataviz skill 8 色類別色板 slot7 violet，與既有 7 色明顯區隔），並修正原七方圖表繪製時發現的版面問題（standalone script，非既有檔案修改）：suptitle/subtitle/子標題三層文字改用 `fig.text`+`subplots_adjust` 分層避免重疊、@1000 端點文字改用「排序後強制最小間距」演算法避免數值相近（如 36.04% vs 35.56%、17.08%/17.02%/16.70%）時文字互疊；繪圖腳本一次性產出未存入 repo
- 10.5 新增 `gen/results/comparison_run10-13-15-17-18-19-9_tagtype_pies_result.png`：在既有 2×3 圓餅圖網格基礎上擴充為 2×4（第 8 格留空），加入 run_9 圓餅圖（pos/pos_semantic 74.2% vs backoff only 25.8%），配色與版面沿用既有慣例；繪圖腳本一次性產出未存入 repo
- 10.6 小結指出 run_9 與 run_19 各 K 差距皆小（@1000 差 0.48pp）、tag type 占比幾乎相同（25.8% vs 25.1% backoff only），但因 run_9 非最終權重，暫不解讀為底模優劣差異；待訓練完成後需回頭更新整節數字

### `docs/reports/comparison_PassLLM_vs_PCFG-LLM_COMB.md` 新增第 9.6／10.7 節：各 @K 已破解密碼的 pos_semantic 標籤比例

- 使用者要求分別針對 run_19（第 9 節）與 run_9（第 10 節）分析「@1/@10/@100/@1000 已破解密碼中，含 pos_semantic 標籤的比例」並用折線圖呈現；經詢問確認比例定義沿用既有 Tag Type 分類規則（密碼 tags 中任一 segment 屬於 pos_semantic 即算含 pos_semantic，不分主次），且兩張圖分別放在對應章節（非合併成單一雙線比較圖）
- 使用者後續要求圖表要有「是／不是 semantic tag」兩條線（原本只畫了「含 pos_semantic」單線），改為兩條互為餘數的線（含 pos_semantic vs 不含 pos_semantic＝backoff/pos，相加 = 100%），兩份報告表格與圖表都已補上「不含 pos_semantic」欄位/線
- 9.6 節（run_19）：含 pos_semantic 比例隨 @K 上升而持續上升（@1=42.96% → @1000=49.33%），不含 pos_semantic 對應下降（57.04% → 50.67%），觀察指出低猜測次數命中的密碼裡結構單純（無語意標籤）的比例反而較高，語意標籤密碼需要更大候選集合才較容易被涵蓋到
- 10.7 節（run_9）：同一分類規則套用在 run_9 結果（含 pos_semantic：41.29% → 48.71%；不含：58.71% → 51.29%），走勢與 run_19 幾乎一致，且各 @K 的差距（0.6–1.7pp）與第 10.3 節 crack rate 的整體差距幅度相近，支持「run_9 落後是整體幅度上的小幅落後、而非特定密碼類型的結構性差異」的推論
- 新增 `gen/results/run_19_Mistral-7B_id7_semantic_ratio_result.png`、`gen/results/run_9_Qwen3-4B_id7_semantic_ratio_result.png`：各一張雙線折線圖（含 pos_semantic 實線 + 不含 pos_semantic 虛線橘色），X 軸 @1/@10/@100/@1000；含 pos_semantic 線配色分別沿用 run_19（navy #1f5c8b）與 run_9（violet #4a3aa7）既有代表色，不含 pos_semantic 線統一用 orange #f0954a；端點標籤改用「依該點兩條線何者較高，動態決定文字往上/往下偏移」的邏輯，避免兩線在 @1000 附近收斂到接近 50/50 時文字互疊；繪圖腳本一次性產出未存入 repo
